name: contrib_grid_example
description: "Example app for the contrib_grid package."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  contrib_grid:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
