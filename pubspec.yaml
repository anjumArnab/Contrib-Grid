name: contrib_grid
description: "Contribution heatmap widget for Flutter. Pass date-value pairs in; get a calendar-aligned colored grid out."
version: 0.1.0
repository: https://github.com/anjumArnab/Contrib-Grid

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
